name: Content OS CI

on:
  push:
    branches:
      - content-os-foundation
  pull_request:
    branches:
      - product-platform

jobs:
  python:
    runs-on: ubuntu-latest
    services:
      postgres:
        image: postgres:17
        env:
          POSTGRES_USER: postgres
          POSTGRES_PASSWORD: postgres
          POSTGRES_DB: autoposter_test
        ports:
          - 5432:5432
        options: >-
          --health-cmd "pg_isready -U postgres -d autoposter_test"
          --health-interval 5s
          --health-timeout 5s
          --health-retries 10
    env:
      AUTOPOSTER_TEST_POSTGRES_URL: postgresql://postgres:postgres@localhost:5432/autoposter_test
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
          cache: pip
      - name: Install Python dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -e ".[dev]"
      - name: Compile Python package
        run: python -m compileall autoposter_bot
      - name: Show discovered crypto/workspace test sources
        shell: bash
        run: |
          echo "HEAD=$(git rev-parse HEAD)"
          find . -type f \( -name 'test_crypto_and_media.py' -o -name '*workspace*.py' -o -name '*crypto*.py' \) -print | sort
          grep -R "test_workspace_account_creation_encrypts_secret_options\|def init_workspace_schema" -n . --exclude-dir=.git || true
      - name: Run tests
        shell: bash
        run: |
          set -o pipefail
          pytest -vv -x --tb=long 2>&1 | tee /tmp/pytest.log || {
            echo "::group::pytest full failure tail"
            tail -n 260 /tmp/pytest.log
            echo "::endgroup::"
            while IFS= read -r line; do
              safe=${line//'%'/'%25'}
              safe=${safe//$'\r'/'%0D'}
              safe=${safe//$'\n'/'%0A'}
              echo "::error title=pytest::$safe"
            done < <(tail -n 180 /tmp/pytest.log)
            exit 1
          }

  web:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: web
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: "24"
      - name: Install web dependencies
        run: npm install
      - name: Typecheck
        run: npm run typecheck
      - name: Build web app
        run: npm run build